# All permissive licences — no AGPL anywhere (PyMuPDF must NOT be added).
pdfplumber>=0.11     # MIT
pypdfium2>=4.30      # BSD/Apache
pytesseract>=0.3.10  # Apache
Pillow>=10.0         # MIT-CMU
openpyxl>=3.1        # MIT
customtkinter>=5.2   # MIT  (modern widget set for the app window)
